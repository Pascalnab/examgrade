# JWT secret for signing session tokens (generate a random 64+ char string)
JWT_SECRET=your-random-jwt-secret-at-least-64-characters-long

# Database connection string (MySQL / TiDB)
DATABASE_URL=mysql://user:password@host:port/database

# Owner's openId for auto-admin (format: email_<nanoid>)
OWNER_OPEN_ID=email_abc123

# LLM API (for AI grading)
BUILT_IN_FORGE_API_URL=https://your-llm-api-url
BUILT_IN_FORGE_API_KEY=your-llm-api-key
